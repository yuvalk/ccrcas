# Credentials and secrets - never commit these
config/.claude/credentials.json
config/.claude/settings.json
config/.claude/projects/
config/.claude.json

# Logs
logs/

# SrvAny binary (not committed by default - download from GitHub releases)
srvany-ng.exe
srvany.exe
